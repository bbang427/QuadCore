<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 선택됐을 때 보여줄 아이콘 -->
    <item
        android:state_checked="true"
        android:drawable="@drawable/ic_community_active" />
    <!-- 그 외(미선택)일 때 보여줄 아이콘 -->
    <item
        android:drawable="@drawable/ic_community_inactive" />

</selector>